# 下载数据

我们将接触两张常见格式： CSV 和 JSON。

# CSV文件

将数据组织以一系列以**逗号分隔的值**（Comma-Separated Values, CSV）存储在纯文本文件中。每行表示一条记录，字段之间用逗号分隔。

`"USW00094728","725030","2020-01-01",32.5,1013.2,5.1,180`

## 解析CSV文件头

CSV在python标准库，用于解析CSV文件的数据行。